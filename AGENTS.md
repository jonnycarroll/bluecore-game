# Agent Instructions

- Keep responses very concise. Prefer the shortest answer that still gives the user the key outcome, verification, and next step when relevant.
- Treat `README.md` as the source of truth for the app structure, current features, run instructions, and project scope.
- Keep changes small and consistent with the current plain HTML/CSS/JavaScript codebase.
- Run `npm test` after changes that touch JavaScript behavior or test coverage.
- If reviving dormant game-layer code such as `points.js`, update `README.md`, loaded scripts, styles, and UI together so the documented active app stays accurate.
